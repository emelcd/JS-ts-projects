{
  "name": "color-flipper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild ./src/index.ts --outdir=build --bundle --watch",
    "build": "esbuild ./src/index.ts --outdir=build --bundle --minify"
  },
  "author": "emelcd",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.0.0",
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/nodemon": "^1.19.1"
  }
}
